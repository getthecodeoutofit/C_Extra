
#define NDEBUG //it will turn off all the insertion assert program/funtion 
#include<stdio.h>
#include<assert.h>

int main(){
        //assert is a fuction used to check many things
        //if the result of assert comes false then the whole program gets terminated/aborted with the error message "assert failed" 
    return 0;
    
}
